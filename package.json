{
  "name": "cerebro-trader",
  "version": "1.0.0",
  "description": "AI-powered on-chain CLOB trading bot — TypeSafe Jev brain on Monad",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "backtest": "bun run src/backtest/index.ts",
    "build": "bun build src/index.ts --outdir dist",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "viem": "^2.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.15",
    "bun-types": "^1.2.15",
    "typescript": "^5.7.3"
  }
}
